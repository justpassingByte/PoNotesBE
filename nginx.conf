events {
    worker_connections 1024;
}

http {
    include       mime.types;
    default_type  application/octet-stream;
    sendfile        on;
    keepalive_timeout  65;
    client_max_body_size 100M;

    # Backend API server
    upstream backend {
        server backend:3001;
    }

    # Frontend UI server
    upstream frontend {
        server frontend:3000;
    }

    server {
        listen 80;
        server_name _; # Match all IPs/Domains

        # 1. API Requests
        location /api/ {
            proxy_pass http://backend/api/;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_cache_bypass $http_upgrade;
            
            # IMPROVED PROXY SETS: Fix for 413 Payload Too Large
            client_max_body_size 100M;
            proxy_request_buffering off;   # Stream directly to backend
            proxy_buffering off;           # No disk caching for proxy
            proxy_read_timeout 300s;      # Long OCR tasks support
            proxy_connect_timeout 300s;
            
            # CORS for direct API access if needed
            add_header 'Access-Control-Allow-Origin' '*' always;
            add_header 'Access-Control-Allow-Credentials' 'true' always;
        }

        # 2. Frontend Requests
        location / {
            proxy_pass http://frontend;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_cache_bypass $http_upgrade;
        }
    }
}
